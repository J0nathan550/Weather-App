<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Weather_App.Pages.AirQualityPage"
             Title="Якість воздуху">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListView x:Name="mainView" ZIndex="1" VerticalOptions="Start" IsPullToRefreshEnabled="True" IsEnabled="False" Refreshing="ListView_Refreshing" Grid.RowSpan="2" HeightRequest="130"/>
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="15" Margin="15" x:Name="mainLayout">

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>